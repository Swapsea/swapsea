<div class="container">
    <div class="full-content-center animated fadeIn extra-margin-top">
        <%= image_tag('alternate-logo.png', :class => "modal-logo") %>
        <div class="box-info exceptions">
          <h2>Request Timeout</h2>
          <% if @exception %>
          	<p><%= @exception.message %></p>
          <% else %>
          	<p>The servers must be struggling to keep up with demand. We'll go out back and tell them to pull their collective socks up.</p>
          <% end %>
    </div>
    <%= link_to "Back", :back %>
    </div>
</div>
