<div class="container">
    <div class="full-content-center animated fadeIn extra-margin-top">
        <%= image_tag('alternate-logo.png', :class => "modal-logo") %>
        <div class="box-info exceptions">
          <h2>Not Found</h2>
          <% if @exception %>
          	<p><%= @exception.message %></p>
          <% else %>
          	<p>This is like a Where's Wally book but there's no Wally. We'll make a note, and fix it soon.</p>
          <% end %>
    </div>
    <%= link_to "Back", :back %>
    </div>
</div>
