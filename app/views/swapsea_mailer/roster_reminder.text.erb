G'day <%= @user.first_name %>,

You have a patrol coming up soon.

Your upcoming patrol:
<%= @next_roster.patrol.name %>
<%= @next_roster.start.strftime("%A %d %B %Y") %>
<%= @next_roster.start.strftime("%H:%M") %> - <%= @next_roster.finish.strftime("%H:%M") %>

<% if @following_roster != nil %>
Your following patrol:
<%= @following_roster.patrol.name %>
<%= @following_roster.start.strftime("%A %d %B %Y") %>
<%= @following_roster.start.strftime("%H:%M") %> - <%= @following_roster.finish.strftime("%H:%M") %>
<% end %>

If you can't make either of these patrols, swap it!
Login to Swapsea at <%= login_url %>

You can also:
- See who else will be there: <%= patrol_roster_url(@next_roster) %>
- Check the weather conditions: https://swell.willyweather.com.au/

Have a great weekend!

Much Love,
Swapsea
